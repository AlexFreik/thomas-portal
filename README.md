# Thomas Portal

## Build

```sh
npm install
npm run css
npm run build
```

## Code Format

```sh
npm run pretty
```

## Run

Run this to start the server, it will run on the port `8000` by default.

```sh
npm run serve
```
